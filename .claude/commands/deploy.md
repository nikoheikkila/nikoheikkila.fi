1. Load the `/terraform` skill.
2. Change to the `infra/` directory
3. Run `task validate` and `task plan`
4. Show the plan output and summarise what resources would change. Do not apply without explicit confirmation.