pre-push:
    commands:
        sync:
            run:
                - task test-unit

pre-commit:
    commands:
        lint:
            run:
                - task format
                - task lint
