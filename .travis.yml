language: node_js

os:
  - linux
  - osx

node_js:
  - "12"

before_install:
  - npm install -g yarn@latest

install:
  - yarn --frozen-lockfile --prefer-offline
  - yarn build

script:
  - yarn lint
  - yarn test:a11y

cache: yarn
